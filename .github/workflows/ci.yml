# ワークフローの名前
name: CI Checks (Example: HTML/CSS/JS Linting) # 名前を実態に合わせて変更

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint: # ジョブ名を変更 (例: lint)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # --- ここから下を削除またはコメントアウト ---
      # - name: Use Node.js ${{ matrix.node-version }}
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: ${{ matrix.node-version }}

      # - name: Install dependencies
      #   run: npm ci # ← これを削除

      # - name: Build project # ← これも不要なら削除
      #   run: npm run build --if-present

      # - name: Run tests
      #   run: npm test # ← これを削除
      # --- ここまで削除 ---

      # 例: もし将来的にHTML/CSS/JSの構文チェック(Lint)などを追加したい場合は、
      # Node.jsセットアップと関連ツール(ESLintなど)のインストール・実行ステップをここに追加します。
      # - name: Setup Node.js for Linting
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '20.x'
      # - name: Install ESLint (example)
      #   run: npm install eslint
      # - name: Run ESLint (example)
      #   run: npx eslint js/**/*.js
